<!doctype html>
<html>
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>NeoBank</title>
<style>
  :root{
    --bg:#0b1220;
    --text:#e7eefc;
    --muted:#a8b3cf;
    --accent:#4f8cff;
    --accent2:#34d399;
    --line:rgba(255,255,255,.08);
    --shadow:0 20px 60px rgba(0,0,0,.35);
    --r:20px;
  }

  *{box-sizing:border-box}

  body{
    margin:0;
    font-family: system-ui,-apple-system,Segoe UI,Roboto,Arial;
    background:
      radial-gradient(1200px 800px at 20% 10%, rgba(79,140,255,.25), transparent 60%),
      radial-gradient(900px 600px at 90% 20%, rgba(52,211,153,.18), transparent 50%),
      linear-gradient(180deg,var(--bg),#060a12);
    color:var(--text);
    min-height:100vh;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:24px;
  }

  .card{
    width:min(460px,100%);
    padding:40px;
    border-radius:var(--r);
    background:linear-gradient(180deg,rgba(18,27,46,.95),rgba(15,23,42,.88));
    border:1px solid var(--line);
    box-shadow:var(--shadow);
    text-align:center;
  }

  .logo{
    width:64px;
    height:64px;
    border-radius:18px;
    margin:0 auto 20px;
    background:linear-gradient(135deg,var(--accent),var(--accent2));
    box-shadow:var(--shadow);
  }

  h1{
    margin:0 0 10px;
    font-size:26px;
    letter-spacing:.3px;
  }

  p{
    margin:0 0 30px;
    color:var(--muted);
    font-size:14px;
  }

  .btn{
    display:inline-block;
    padding:14px 28px;
    border-radius:14px;
    background:linear-gradient(135deg,var(--accent),#3b6eea);
    color:white;
    font-weight:600;
    text-decoration:none;
    transition:all .2s ease;
    box-shadow:0 10px 25px rgba(79,140,255,.4);
  }

  .btn:hover{
    transform:translateY(-2px);
    box-shadow:0 15px 35px rgba(79,140,255,.5);
  }

  .footer{
    margin-top:24px;
    font-size:12px;
    color:var(--muted);
  }
</style>
</head>
<body>

  <div class="card">
    <div class="logo"></div>
    <h1>Welcome to NeoBank</h1>
    <p>Secure digital banking powered by OAuth2 and Keycloak.</p>

    <a href="<%= authUrl %>" class="btn">
      🚀 Get Started with NeoBank
    </a>

    <div class="footer">
      Authorization Code Flow • Secure Login
    </div>
  </div>

</body>
</html>
