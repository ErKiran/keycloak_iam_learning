<!doctype html>
<html>
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>Transfer Blocked</title>
<style>
  :root{
    --bg:#0b1220;--text:#e7eefc;--muted:#a8b3cf;--line:rgba(255,255,255,.08);
    --accent:#4f8cff;--bad:#fb7185;--shadow:0 20px 60px rgba(0,0,0,.35);--r:20px;
  }
  *{box-sizing:border-box}
  body{
    margin:0;
    font-family: system-ui,-apple-system,Segoe UI,Roboto,Arial;
    background:
      radial-gradient(1200px 800px at 20% 10%, rgba(79,140,255,.25), transparent 60%),
      radial-gradient(900px 600px at 90% 20%, rgba(251,113,133,.15), transparent 50%),
      linear-gradient(180deg,var(--bg),#060a12);
    color:var(--text);
    min-height:100vh;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:24px;
  }
  .card{
    width:min(520px,100%);
    padding:36px;
    border-radius:var(--r);
    background:linear-gradient(180deg,rgba(18,27,46,.95),rgba(15,23,42,.88));
    border:1px solid var(--line);
    box-shadow:var(--shadow);
  }
  .top{
    display:flex;align-items:center;gap:14px;margin-bottom:16px;
  }
  .icon{
    width:46px;height:46px;border-radius:16px;
    background:rgba(251,113,133,.18);
    border:1px solid rgba(251,113,133,.35);
    display:flex;align-items:center;justify-content:center;
    box-shadow: 0 12px 35px rgba(0,0,0,.25);
    font-size:20px;
  }
  h1{margin:0;font-size:24px;letter-spacing:.2px}
  p{margin:10px 0 0;color:var(--muted);line-height:1.5}
  .alert{
    margin-top:18px;
    padding:14px;
    border-radius:16px;
    border:1px solid rgba(251,113,133,.25);
    background:rgba(251,113,133,.10);
    color:rgba(231,238,252,.95);
  }
  .actions{display:flex;gap:10px;margin-top:22px;flex-wrap:wrap}
  a.btn{
    display:inline-block;
    padding:12px 16px;
    border-radius:14px;
    text-decoration:none;
    font-weight:600;
    border:1px solid var(--line);
    background:rgba(255,255,255,.06);
    color:var(--text);
  }
  a.btn.primary{
    background:linear-gradient(135deg,rgba(79,140,255,.95),rgba(79,140,255,.65));
    border:none;
  }
  .small{margin-top:14px;font-size:12px;color:var(--muted)}
</style>
</head>
<body>
  <div class="card">
    <div class="top">
      <div class="icon">⛔</div>
      <div>
        <h1>Transfer Blocked</h1>
        <p class="small">Authorization required</p>
      </div>
    </div>

    <div class="alert">
      <strong>You aren't authorized to perform this action. !!</strong>
      <div class="small" style="margin-top:8px">
        This usually means your account doesn’t have the required permission.
      </div>
    </div>

    <div class="actions">
      <a class="btn primary" href="/dashboard">Back to Dashboard</a>
      <a class="btn" href="/">Home</a>
    </div>
  </div>
</body>
</html>`
